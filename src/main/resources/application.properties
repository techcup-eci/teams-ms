# spring.application.name=Servicio-TeamReadbull

# # Base de datos
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# # JWT
# jwt.secret=${JWT_SECRET}
# jwt.expiration=${JWT_EXPIRATION}

# # Otros servicios
# tournament-service.url=${TOURNAMENT_SERVICE_URL}
# user-service.url=${USER_SERVICE_URL:http://localhost:8082}

# # Puerto
# server.port=8082

# # API Gateway
# gateway.url=${GATEWAY_URL:http://localhost:8080}




spring.application.name=Servicio-TeamReadbull

# Inter-service: users-and-players-ms (via API Gateway)
user-service.url=${USER_SERVICE_URL:http://localhost:8082}

# Base de datos
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/techcup_teams}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT — solo el Gateway valida JWT. Este microservicio solo lee headers X-User-Id y X-User-Role.
# El secret se mantiene por si se necesita en el futuro, pero NO se usa para validar tokens.
jwt.secret=${JWT_SECRET:TechCupSecretKey2026DefaultOnlyForDev}
jwt.expiration=${JWT_EXPIRATION:900000}

# Otros servicios (via API Gateway)
tournament-service.url=${TOURNAMENT_SERVICE_URL:http://localhost:8088}

# Puerto
server.port=8088

## Api Gateway
gateway.url=${GATEWAY_URL:http://localhost:8081}

